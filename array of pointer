#include<stdio.h>
int main()
{
    int a[5]={1,2,3,4,5}; //array a is declared and initialized with 5 elements
    int* p[5]; //pointer array is declared with size 5
    for(int i=0;i<5;i++)
    {
        p[i]=&a[i]; //address of a is assigning to p
        printf("adress stored in p is:%p \n",p[i]);//printing a address that is stored in p
    }
}

output:

adress stored in p is:0x7ffdf2a2af70 
adress stored in p is:0x7ffdf2a2af74 
adress stored in p is:0x7ffdf2a2af78 
adress stored in p is:0x7ffdf2a2af7c 
adress stored in p is:0x7ffdf2a2af80 
