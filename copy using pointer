#include<stdio.h>
int main()
{
    int a[5]={1,2,3,4,5};//array a is decalared and initialized with five elements
    int *p[5];//pointer array p is decalared with size five
    for(int i=0;i<5;i++)
    {
        //printf("elements of a first array a is: %d \n",a[i]); //it prints the array elements by using array indexes
        printf("elements of a first array a is: %d \n",*(a+i)); //it prints the array elements by using array modifier
    }
    for(int i=0;i<5;i++)
    {
        p[i]=&a[i];
        //printf(" \t elements of a second array p is: %d \n",*(p[i])); //it prints the array elements by using the pointer array indexes
        printf("\t elements of a second array p is: %d \n",**(p+i)); //it prints the array elements by using the pointer array modifier
    }
}

output:

elements of a first array a is: 1 
elements of a first array a is: 2 
elements of a first array a is: 3 
elements of a first array a is: 4 
elements of a first array a is: 5 
         elements of a second array p is: 1 
         elements of a second array p is: 2 
         elements of a second array p is: 3 
         elements of a second array p is: 4 
         elements of a second array p is: 5 
